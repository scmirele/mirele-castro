import { render } from '@/tests/utils'

import { Wrapper } from './Wrapper'

describe('Wrapper Component', () => {
  it('should render correctly', () => {
    render(<Wrapper>teste</Wrapper>)
  })
})
