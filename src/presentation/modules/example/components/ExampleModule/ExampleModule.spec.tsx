import { render } from '@/tests/utils'

import { ExampleModule } from './ExampleModule'

describe('ExampleModule Component', () => {
  it('should render correctly', () => {
    render(<ExampleModule />)
  })
})
